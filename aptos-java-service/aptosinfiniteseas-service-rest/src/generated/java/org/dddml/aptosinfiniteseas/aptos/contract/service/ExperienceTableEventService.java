// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.aptosinfiniteseas.aptos.contract.service;

import com.github.wubuku.aptos.bean.Event;
import com.github.wubuku.aptos.utils.NodeApiClient;

import org.dddml.aptosinfiniteseas.domain.experiencetable.AbstractExperienceTableEvent;
import org.dddml.aptosinfiniteseas.aptos.contract.ContractConstants;
import org.dddml.aptosinfiniteseas.aptos.contract.DomainBeanUtils;
import org.dddml.aptosinfiniteseas.aptos.contract.AptosAccount;

import org.dddml.aptosinfiniteseas.aptos.contract.experiencetable.ExperienceTableInitialized;
import org.dddml.aptosinfiniteseas.aptos.contract.experiencetable.ExperienceLevelAdded;
import org.dddml.aptosinfiniteseas.aptos.contract.experiencetable.ExperienceLevelUpdated;
import org.dddml.aptosinfiniteseas.aptos.contract.repository.ExperienceTableEventRepository;
import org.dddml.aptosinfiniteseas.aptos.contract.repository.AptosAccountRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.io.IOException;
import java.math.*;
import java.util.*;


@Service
public class ExperienceTableEventService {

    @Value("${aptos.contract.address}")
    private String aptosContractAddress;

    @Autowired
    private AptosAccountRepository aptosAccountRepository;

    @Autowired
    private NodeApiClient aptosNodeApiClient;

    @Autowired
    private ExperienceTableEventRepository experienceTableEventRepository;

    @Transactional
    public void updateStatusToProcessed(AbstractExperienceTableEvent event) {
        event.setEventStatus("D");
        experienceTableEventRepository.save(event);
    }

    @Transactional
    public void pullExperienceLevelAddedEvents() {
        String resourceAccountAddress = getResourceAccountAddress();
        if (resourceAccountAddress == null) {
            return;
        }
        int limit = 1;
        BigInteger cursor = getExperienceLevelAddedEventNextCursor();
        if (cursor == null) {
            cursor = BigInteger.ZERO;
        }
        while (true) {
            List<Event<ExperienceLevelAdded>> eventPage;
            try {
                eventPage = aptosNodeApiClient.getEventsByEventHandle(
                        resourceAccountAddress,
                        this.aptosContractAddress + "::" + ContractConstants.EXPERIENCE_TABLE_MODULE_EVENTS,
                        ContractConstants.EXPERIENCE_TABLE_MODULE_EXPERIENCE_LEVEL_ADDED_HANDLE_FIELD,
                        ExperienceLevelAdded.class,
                        cursor.longValue(),
                        limit
                );
            } catch (IOException e) {
                throw new RuntimeException(e);
            }

            if (eventPage != null && eventPage.size() > 0) {
                cursor = cursor.add(BigInteger.ONE);
                for (Event<ExperienceLevelAdded> eventEnvelope : eventPage) {
                    eventEnvelope.getData().setAccountAddress(resourceAccountAddress);
                    saveExperienceLevelAdded(eventEnvelope);
                }
            } else {
                break;
            }
        }
    }

    private BigInteger getExperienceLevelAddedEventNextCursor() {
        AbstractExperienceTableEvent.ExperienceLevelAdded lastEvent = experienceTableEventRepository.findFirstExperienceLevelAddedByOrderByAptosEventSequenceNumber();
        return lastEvent != null ? lastEvent.getAptosEventSequenceNumber() : null;
    }

    private void saveExperienceLevelAdded(Event<ExperienceLevelAdded> eventEnvelope) {
        AbstractExperienceTableEvent.ExperienceLevelAdded experienceLevelAdded = DomainBeanUtils.toExperienceLevelAdded(eventEnvelope);
        if (experienceTableEventRepository.findById(experienceLevelAdded.getExperienceTableEventId()).isPresent()) {
            return;
        }
        experienceTableEventRepository.save(experienceLevelAdded);
    }

    @Transactional
    public void pullExperienceLevelUpdatedEvents() {
        String resourceAccountAddress = getResourceAccountAddress();
        if (resourceAccountAddress == null) {
            return;
        }
        int limit = 1;
        BigInteger cursor = getExperienceLevelUpdatedEventNextCursor();
        if (cursor == null) {
            cursor = BigInteger.ZERO;
        }
        while (true) {
            List<Event<ExperienceLevelUpdated>> eventPage;
            try {
                eventPage = aptosNodeApiClient.getEventsByEventHandle(
                        resourceAccountAddress,
                        this.aptosContractAddress + "::" + ContractConstants.EXPERIENCE_TABLE_MODULE_EVENTS,
                        ContractConstants.EXPERIENCE_TABLE_MODULE_EXPERIENCE_LEVEL_UPDATED_HANDLE_FIELD,
                        ExperienceLevelUpdated.class,
                        cursor.longValue(),
                        limit
                );
            } catch (IOException e) {
                throw new RuntimeException(e);
            }

            if (eventPage != null && eventPage.size() > 0) {
                cursor = cursor.add(BigInteger.ONE);
                for (Event<ExperienceLevelUpdated> eventEnvelope : eventPage) {
                    eventEnvelope.getData().setAccountAddress(resourceAccountAddress);
                    saveExperienceLevelUpdated(eventEnvelope);
                }
            } else {
                break;
            }
        }
    }

    private BigInteger getExperienceLevelUpdatedEventNextCursor() {
        AbstractExperienceTableEvent.ExperienceLevelUpdated lastEvent = experienceTableEventRepository.findFirstExperienceLevelUpdatedByOrderByAptosEventSequenceNumber();
        return lastEvent != null ? lastEvent.getAptosEventSequenceNumber() : null;
    }

    private void saveExperienceLevelUpdated(Event<ExperienceLevelUpdated> eventEnvelope) {
        AbstractExperienceTableEvent.ExperienceLevelUpdated experienceLevelUpdated = DomainBeanUtils.toExperienceLevelUpdated(eventEnvelope);
        if (experienceTableEventRepository.findById(experienceLevelUpdated.getExperienceTableEventId()).isPresent()) {
            return;
        }
        experienceTableEventRepository.save(experienceLevelUpdated);
    }

    private String getResourceAccountAddress() {
        return aptosAccountRepository.findById(ContractConstants.RESOURCE_ACCOUNT_ADDRESS)
                .map(AptosAccount::getAddress).orElse(null);
    }
}
