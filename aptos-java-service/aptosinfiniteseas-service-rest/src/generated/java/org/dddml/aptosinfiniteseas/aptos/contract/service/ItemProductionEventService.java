// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.aptosinfiniteseas.aptos.contract.service;

import com.github.wubuku.aptos.bean.Event;
import com.github.wubuku.aptos.utils.NodeApiClient;

import org.dddml.aptosinfiniteseas.domain.itemproduction.AbstractItemProductionEvent;
import org.dddml.aptosinfiniteseas.aptos.contract.ContractConstants;
import org.dddml.aptosinfiniteseas.aptos.contract.DomainBeanUtils;
import org.dddml.aptosinfiniteseas.aptos.contract.AptosAccount;

import org.dddml.aptosinfiniteseas.aptos.contract.itemproduction.ItemProductionCreated;
import org.dddml.aptosinfiniteseas.aptos.contract.itemproduction.ItemProductionUpdated;
import org.dddml.aptosinfiniteseas.aptos.contract.repository.ItemProductionEventRepository;
import org.dddml.aptosinfiniteseas.aptos.contract.repository.AptosAccountRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.io.IOException;
import java.math.*;
import java.util.*;


@Service
public class ItemProductionEventService {

    @Value("${aptos.contract.address}")
    private String aptosContractAddress;

    @Autowired
    private AptosAccountRepository aptosAccountRepository;

    @Autowired
    private NodeApiClient aptosNodeApiClient;

    @Autowired
    private ItemProductionEventRepository itemProductionEventRepository;

    @Transactional
    public void updateStatusToProcessed(AbstractItemProductionEvent event) {
        event.setEventStatus("D");
        itemProductionEventRepository.save(event);
    }

    @Transactional
    public void pullItemProductionCreatedEvents() {
        String resourceAccountAddress = getResourceAccountAddress();
        if (resourceAccountAddress == null) {
            return;
        }
        int limit = 1;
        BigInteger cursor = getItemProductionCreatedEventNextCursor();
        if (cursor == null) {
            cursor = BigInteger.ZERO;
        }
        while (true) {
            List<Event<ItemProductionCreated>> eventPage;
            try {
                eventPage = aptosNodeApiClient.getEventsByEventHandle(
                        resourceAccountAddress,
                        this.aptosContractAddress + "::" + ContractConstants.ITEM_PRODUCTION_MODULE_EVENTS,
                        ContractConstants.ITEM_PRODUCTION_MODULE_ITEM_PRODUCTION_CREATED_HANDLE_FIELD,
                        ItemProductionCreated.class,
                        cursor.longValue(),
                        limit
                );
            } catch (IOException e) {
                throw new RuntimeException(e);
            }

            if (eventPage != null && eventPage.size() > 0) {
                cursor = cursor.add(BigInteger.ONE);
                for (Event<ItemProductionCreated> eventEnvelope : eventPage) {
                    saveItemProductionCreated(eventEnvelope);
                }
            } else {
                break;
            }
        }
    }

    private BigInteger getItemProductionCreatedEventNextCursor() {
        AbstractItemProductionEvent.ItemProductionCreated lastEvent = itemProductionEventRepository.findFirstItemProductionCreatedByOrderByAptosEventSequenceNumber();
        return lastEvent != null ? lastEvent.getAptosEventSequenceNumber() : null;
    }

    private void saveItemProductionCreated(Event<ItemProductionCreated> eventEnvelope) {
        AbstractItemProductionEvent.ItemProductionCreated itemProductionCreated = DomainBeanUtils.toItemProductionCreated(eventEnvelope);
        if (itemProductionEventRepository.findById(itemProductionCreated.getItemProductionEventId()).isPresent()) {
            return;
        }
        itemProductionEventRepository.save(itemProductionCreated);
    }

    @Transactional
    public void pullItemProductionUpdatedEvents() {
        String resourceAccountAddress = getResourceAccountAddress();
        if (resourceAccountAddress == null) {
            return;
        }
        int limit = 1;
        BigInteger cursor = getItemProductionUpdatedEventNextCursor();
        if (cursor == null) {
            cursor = BigInteger.ZERO;
        }
        while (true) {
            List<Event<ItemProductionUpdated>> eventPage;
            try {
                eventPage = aptosNodeApiClient.getEventsByEventHandle(
                        resourceAccountAddress,
                        this.aptosContractAddress + "::" + ContractConstants.ITEM_PRODUCTION_MODULE_EVENTS,
                        ContractConstants.ITEM_PRODUCTION_MODULE_ITEM_PRODUCTION_UPDATED_HANDLE_FIELD,
                        ItemProductionUpdated.class,
                        cursor.longValue(),
                        limit
                );
            } catch (IOException e) {
                throw new RuntimeException(e);
            }

            if (eventPage != null && eventPage.size() > 0) {
                cursor = cursor.add(BigInteger.ONE);
                for (Event<ItemProductionUpdated> eventEnvelope : eventPage) {
                    saveItemProductionUpdated(eventEnvelope);
                }
            } else {
                break;
            }
        }
    }

    private BigInteger getItemProductionUpdatedEventNextCursor() {
        AbstractItemProductionEvent.ItemProductionUpdated lastEvent = itemProductionEventRepository.findFirstItemProductionUpdatedByOrderByAptosEventSequenceNumber();
        return lastEvent != null ? lastEvent.getAptosEventSequenceNumber() : null;
    }

    private void saveItemProductionUpdated(Event<ItemProductionUpdated> eventEnvelope) {
        AbstractItemProductionEvent.ItemProductionUpdated itemProductionUpdated = DomainBeanUtils.toItemProductionUpdated(eventEnvelope);
        if (itemProductionEventRepository.findById(itemProductionUpdated.getItemProductionEventId()).isPresent()) {
            return;
        }
        itemProductionEventRepository.save(itemProductionUpdated);
    }

    private String getResourceAccountAddress() {
        return aptosAccountRepository.findById(ContractConstants.RESOURCE_ACCOUNT_ADDRESS)
                .map(AptosAccount::getAddress).orElse(null);
    }
}
