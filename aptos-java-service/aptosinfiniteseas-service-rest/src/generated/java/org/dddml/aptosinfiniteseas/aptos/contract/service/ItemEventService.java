// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddml.aptosinfiniteseas.aptos.contract.service;

import com.github.wubuku.aptos.bean.Event;
import com.github.wubuku.aptos.utils.NodeApiClient;

import org.dddml.aptosinfiniteseas.domain.item.AbstractItemEvent;
import org.dddml.aptosinfiniteseas.aptos.contract.ContractConstants;
import org.dddml.aptosinfiniteseas.aptos.contract.DomainBeanUtils;
import org.dddml.aptosinfiniteseas.aptos.contract.AptosAccount;

import org.dddml.aptosinfiniteseas.aptos.contract.item.ItemCreated;
import org.dddml.aptosinfiniteseas.aptos.contract.item.ItemUpdated;
import org.dddml.aptosinfiniteseas.aptos.contract.repository.ItemEventRepository;
import org.dddml.aptosinfiniteseas.aptos.contract.repository.AptosAccountRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.io.IOException;
import java.math.*;
import java.util.*;


@Service
public class ItemEventService {

    @Value("${aptos.contract.address}")
    private String aptosContractAddress;

    @Autowired
    private AptosAccountRepository aptosAccountRepository;

    @Autowired
    private NodeApiClient aptosNodeApiClient;

    @Autowired
    private ItemEventRepository itemEventRepository;

    @Transactional
    public void updateStatusToProcessed(AbstractItemEvent event) {
        event.setEventStatus("D");
        itemEventRepository.save(event);
    }

    @Transactional
    public void pullItemCreatedEvents() {
        String resourceAccountAddress = getResourceAccountAddress();
        if (resourceAccountAddress == null) {
            return;
        }
        int limit = 1;
        BigInteger cursor = getItemCreatedEventNextCursor();
        if (cursor == null) {
            cursor = BigInteger.ZERO;
        }
        while (true) {
            List<Event<ItemCreated>> eventPage;
            try {
                eventPage = aptosNodeApiClient.getEventsByEventHandle(
                        resourceAccountAddress,
                        this.aptosContractAddress + "::" + ContractConstants.ITEM_MODULE_EVENTS,
                        ContractConstants.ITEM_MODULE_ITEM_CREATED_HANDLE_FIELD,
                        ItemCreated.class,
                        cursor.longValue(),
                        limit
                );
            } catch (IOException e) {
                throw new RuntimeException(e);
            }

            if (eventPage != null && eventPage.size() > 0) {
                cursor = cursor.add(BigInteger.ONE);
                for (Event<ItemCreated> eventEnvelope : eventPage) {
                    saveItemCreated(eventEnvelope);
                }
            } else {
                break;
            }
        }
    }

    private BigInteger getItemCreatedEventNextCursor() {
        AbstractItemEvent.ItemCreated lastEvent = itemEventRepository.findFirstItemCreatedByOrderByAptosEventSequenceNumber();
        return lastEvent != null ? lastEvent.getAptosEventSequenceNumber() : null;
    }

    private void saveItemCreated(Event<ItemCreated> eventEnvelope) {
        AbstractItemEvent.ItemCreated itemCreated = DomainBeanUtils.toItemCreated(eventEnvelope);
        if (itemEventRepository.findById(itemCreated.getItemEventId()).isPresent()) {
            return;
        }
        itemEventRepository.save(itemCreated);
    }

    @Transactional
    public void pullItemUpdatedEvents() {
        String resourceAccountAddress = getResourceAccountAddress();
        if (resourceAccountAddress == null) {
            return;
        }
        int limit = 1;
        BigInteger cursor = getItemUpdatedEventNextCursor();
        if (cursor == null) {
            cursor = BigInteger.ZERO;
        }
        while (true) {
            List<Event<ItemUpdated>> eventPage;
            try {
                eventPage = aptosNodeApiClient.getEventsByEventHandle(
                        resourceAccountAddress,
                        this.aptosContractAddress + "::" + ContractConstants.ITEM_MODULE_EVENTS,
                        ContractConstants.ITEM_MODULE_ITEM_UPDATED_HANDLE_FIELD,
                        ItemUpdated.class,
                        cursor.longValue(),
                        limit
                );
            } catch (IOException e) {
                throw new RuntimeException(e);
            }

            if (eventPage != null && eventPage.size() > 0) {
                cursor = cursor.add(BigInteger.ONE);
                for (Event<ItemUpdated> eventEnvelope : eventPage) {
                    saveItemUpdated(eventEnvelope);
                }
            } else {
                break;
            }
        }
    }

    private BigInteger getItemUpdatedEventNextCursor() {
        AbstractItemEvent.ItemUpdated lastEvent = itemEventRepository.findFirstItemUpdatedByOrderByAptosEventSequenceNumber();
        return lastEvent != null ? lastEvent.getAptosEventSequenceNumber() : null;
    }

    private void saveItemUpdated(Event<ItemUpdated> eventEnvelope) {
        AbstractItemEvent.ItemUpdated itemUpdated = DomainBeanUtils.toItemUpdated(eventEnvelope);
        if (itemEventRepository.findById(itemUpdated.getItemEventId()).isPresent()) {
            return;
        }
        itemEventRepository.save(itemUpdated);
    }

    private String getResourceAccountAddress() {
        return aptosAccountRepository.findById(ContractConstants.RESOURCE_ACCOUNT_ADDRESS)
                .map(AptosAccount::getAddress).orElse(null);
    }
}
